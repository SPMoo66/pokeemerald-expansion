mapscripts WellspringChasm_1_MapScripts {
	MAP_SCRIPT_ON_TRANSITION {
		setflag (FLAG_LANDMARK_WELLSPRING_CHASM)
	}
}

script WellspringChasm_1_EventScript_Sailor {
	msgbox ("I heard a huge roar from deeper in!\pProbably one of the trainers letting\nloose…?\lAh, let's hope it was only that.", MSGBOX_NPC)
}
